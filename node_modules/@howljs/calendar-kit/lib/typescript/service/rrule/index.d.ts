import type { DateTime } from 'luxon';
import type { DateType } from '../../types';
export declare class RRuleGenerator {
    private rule;
    private dtstart;
    constructor(rrule: string, dtstart: DateType, exDates?: DateType[]);
    generateOccurrences(start: DateType, end: DateType, timeZone?: string): DateTime[];
    firstOccurrence(timeZone?: string): DateTime | null;
}
//# sourceMappingURL=index.d.ts.map