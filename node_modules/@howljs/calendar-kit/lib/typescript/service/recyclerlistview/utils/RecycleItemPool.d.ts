/**
 * Recycle pool for maintaining recyclable items Availability check, add/remove
 * etc are all O(1), uses two maps to achieve constant time operation
 */
export default class RecycleItemPool {
    private _recyclableObjectSet;
    private _availabilitySet;
    constructor();
    putRecycledObject(object: string): void;
    getRecycledObject(): string | undefined;
    removeFromPool(object: string): boolean;
    clearAll(): void;
}
//# sourceMappingURL=RecycleItemPool.d.ts.map