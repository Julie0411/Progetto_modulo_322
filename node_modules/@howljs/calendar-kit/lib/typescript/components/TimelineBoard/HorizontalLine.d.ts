import React from 'react';
import type { SharedValue } from 'react-native-reanimated';
interface HorizontalLineProps {
    borderColor: string;
    index: number;
    height: SharedValue<number>;
    renderCustomHorizontalLine?: (props: {
        index: number;
        borderColor: string;
    }) => React.ReactNode;
}
declare const HorizontalLine: ({ index, borderColor, height, renderCustomHorizontalLine, }: HorizontalLineProps) => React.JSX.Element;
export default HorizontalLine;
//# sourceMappingURL=HorizontalLine.d.ts.map